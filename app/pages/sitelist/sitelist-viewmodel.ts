import observable = require("data/observable");
import dialogs = require("ui/dialogs");

export class SiteListViewModel extends observable.Observable {
      
    constructor() {
        super();
    }  
}